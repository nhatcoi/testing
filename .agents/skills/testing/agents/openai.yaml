interface:
  display_name: "Testing"
  short_description: "Repo-specific patterns and workflows for testing"
  default_prompt: "Use the testing repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true